const books = require('./book')
// const otp = require('./otp')
// const users = require('./user')
const genres = require('./genreSchema')

module.exports = {
  books,
  genres,
  //   otp,
  //   users,
}
